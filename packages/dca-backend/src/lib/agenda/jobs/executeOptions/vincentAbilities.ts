// import { OptionsToolClient } from '@ahumblebuilder/vincent-ability-options';

// export function getOptionsToolClient(): OptionsToolClient {
//   return new OptionsToolClient();
// }

// Temporary placeholder until the package is available
export function getOptionsToolClient(): any {
  throw new Error('Options ability package not yet available');
}
